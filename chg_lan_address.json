{ 
   "secondary": [
   {
      "address": "{{ old_cfg.ip }}",
      "mask":    "{{ old_cfg.mask }}",
      "secondary": [null]
   }
    ],
   "primary": 
   {
      "address": "{{ new_cfg.ip }}",
      "mask":    "{{ new_cfg.mask }}"
   }    
}

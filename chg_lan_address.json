{ 
   "secondary": [
   {
      "address": "{{ old_cfg.primary.address }}",
      "mask":    "{{ old_cfg.primary.mask }}",
      "secondary": [null]
   }
    ],
   "primary": 
   {
      "address": "{{ new_cfg.address }}",
      "mask":    "{{ new_cfg.mask }}"
   }    
}
